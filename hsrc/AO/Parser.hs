
-- | Parser for AO code.
--
-- This is just the code parser. It does not parse full dictionary
-- files. This uses Parsec, rather than Read, to ensure users have
-- better error information.
--
module AO.Parser
    (
    ) where



